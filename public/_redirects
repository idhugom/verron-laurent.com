# Redirections Cloudflare Pages (basées sur le chemin)
# La redirection apex (sans www) -> www est gérée par functions/_middleware.js
# (les règles _redirects de Pages ne matchent pas sur le nom d'hôte).

# Anciens flux WordPress -> RSS
/feed        /rss.xml   301
/feed/       /rss.xml   301
/comments/feed/  /rss.xml  301
